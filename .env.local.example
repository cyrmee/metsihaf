SUPABASE_URL=
SUPABASE_ANON_KEY=

# Pooled/transaction-mode Postgres connection (used by the Prisma client).
DATABASE_URL=
# Direct/session-mode Postgres connection (used only for `prisma migrate`/introspection).
DIRECT_URL=
